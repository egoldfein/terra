import { shallow } from "enzyme";
import PlantList from "./PlantList";

describe("<PlantList />", () => {
  it("should render PlantList component", () => {
    // const wrapper = shallow(<PlantList plants={[]} loading={false} />);
    // expect(wrapper.exists()).toBeTruthy();
  });
});
